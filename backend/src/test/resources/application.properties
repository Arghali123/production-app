# Tests use an in-memory database so they do not require Docker or PostgreSQL.
spring.datasource.url=jdbc:h2:mem:backend-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
